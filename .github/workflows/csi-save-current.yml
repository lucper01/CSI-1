name: Save current CSI checkpoint
on:
  push:
    paths:
      - '.github/workflows/csi-save-current.yml'
permissions:
  contents: write
jobs:
  save:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Copy current presentation to frozen save
        run: |
          cp index.html index_save.html
          cmp -s index.html index_save.html
      - name: Commit save checkpoint
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add index_save.html
          git commit -m "Save current CSI presentation checkpoint"
          git push
